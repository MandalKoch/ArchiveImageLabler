name: archiveimagelabler

services:
  archiveimagelabler:
    build:
      context: .
      dockerfile: src/Dockerfile
    ports:
      - "127.0.0.1:${ARCHIVEIMAGELABLER_HTTP_PORT:-5080}:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Library__RootPath: /library
      Library__DataPath: /app/data
      Library__ScanParallelism: ${Library__ScanParallelism:-4}
    volumes:
      - type: bind
        source: ${ARCHIVEIMAGELABLER_LIBRARY_PATH:?Set ARCHIVEIMAGELABLER_LIBRARY_PATH in .env}
        target: /library
        read_only: true
      - type: bind
        source: ${ARCHIVEIMAGELABLER_DATA_PATH:?Set ARCHIVEIMAGELABLER_DATA_PATH in .env}
        target: /app/data
